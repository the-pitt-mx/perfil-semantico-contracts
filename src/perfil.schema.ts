// Fase 0 — placeholder de estructura. Sin lógica todavía.
// Responsabilidad: PerfilSemantico, versión de perfil, estado del perfil.
// Se implementa en: Fase 1 (ver ADR-001 §8).
// Decisión pendiente de Fase 1: usar Zod como fuente única y derivar
// tipos TypeScript + JSON Schema, en vez de mantener ambos a mano.
export {};
